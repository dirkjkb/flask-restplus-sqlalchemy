# FLASK-RESTPLUS and SQLAlchemy
This is a minimal Application that combines a Rest Endpoint with a Postgres Database.

## Run
To simply run the project you just have to run the following command:
```sh
docker-compose up
```
